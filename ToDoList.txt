#!ToDoList.txt

Features:
* Prettily-formatted devices list, rather than raw Python list for cmd "devs"
* Rename event.SECONDS to event.TICK to avoid confusion
* Setup reporting for temperature, if it exists
* Set On/Off reporting, if it exists
* Multiple endpoints?
* Web interface(!)

Bugs

